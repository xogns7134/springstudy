<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
    <insert id="insertMember" parameterType="MemberVO">
        INSERT INTO member (memberID, memberName, email, password, memberNumber, memberAddress)
        VALUES (#{memberID}, #{memberName}, #{email}, #{password}, #{memberNumber}, #{memberAddress})
    </insert>
    <select id="oneMember" resultType="MemberVO">
        SELECT * FROM member WHERE memberID = #{memberID}
    </select>
    <update id="updateMember" parameterType="MemberVO">
        UPDATE member
        SET memberName = #{memberName}, email = #{email}, password = #{password},
            memberNumber = #{memberNumber}, memberAddress = #{memberAddress}
        WHERE memberID = #{memberID}
    </update>
    <delete id="deleteMember" parameterType="String">
        DELETE FROM member WHERE memberID = #{memberID}
    </delete>
    <select id="listMember" resultType="MemberVO">
        SELECT * FROM member
    </select>
</mapper>